version: '2.0'
services:
    nginx:
        restart: always
        image: nginx
        container_name: nginx
        ports:
            - 443:443
            - 80:80
        volumes:
            - ./nginx/conf.d/:/etc/nginx/conf.d
            - ./nginx/logs/:/etc/nginx/logs
            - ./nginx/cert:/etc/nginx/cert
            - ./dist:/var/www/html
            - ./dist/images:/static
